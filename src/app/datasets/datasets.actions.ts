import { createAction } from '@ngrx/store'

export const updateData = createAction('[Datasets Component] Update data')
export const selectDataset = createAction('[Datasets Component] Select dataset')
